name: Trigger Site Deploys

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  trigger-deploy-hooks:
    runs-on: ubuntu-latest
    steps:
      - name: Validate hook configuration
        env:
          NETLIFY_BUILD_HOOK: ${{ secrets.NETLIFY_BUILD_HOOK }}
          CLOUDFLARE_DEPLOY_HOOK: ${{ secrets.CLOUDFLARE_DEPLOY_HOOK }}
          IOP_CLOUDFLARE_DEPLOY_HOOK: ${{ secrets.IOP_CLOUDFLARE_DEPLOY_HOOK }}
        run: |
          if [ -z "$NETLIFY_BUILD_HOOK" ]; then
            echo "NETLIFY_BUILD_HOOK is not set"
            exit 1
          fi

          if [ -z "$CLOUDFLARE_DEPLOY_HOOK" ]; then
            echo "CLOUDFLARE_DEPLOY_HOOK is not set"
            exit 1
          fi

          if [ -z "$IOP_CLOUDFLARE_DEPLOY_HOOK" ]; then
            echo "IOP_CLOUDFLARE_DEPLOY_HOOK is not set"
            exit 1
          fi

      - name: Trigger Netlify build hook
        env:
          NETLIFY_BUILD_HOOK: ${{ secrets.NETLIFY_BUILD_HOOK }}
        run: curl --fail --show-error --silent -X POST -d '{}' "$NETLIFY_BUILD_HOOK"

      - name: Trigger Cloudflare Pages deploy hook
        env:
          CLOUDFLARE_DEPLOY_HOOK: ${{ secrets.CLOUDFLARE_DEPLOY_HOOK }}
        run: curl --fail --show-error --silent -X POST "$CLOUDFLARE_DEPLOY_HOOK"

      - name: Trigger IOP Cloudflare Pages deploy hook
        env:
          IOP_CLOUDFLARE_DEPLOY_HOOK: ${{ secrets.IOP_CLOUDFLARE_DEPLOY_HOOK }}
        run: curl --fail --show-error --silent -X POST "$IOP_CLOUDFLARE_DEPLOY_HOOK"
